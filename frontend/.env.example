# URL base do backend fornecido (já hospedado). Copie este arquivo para .env.local.
# Hospedado (padrão):
NEXT_PUBLIC_API_URL=https://8tymn68hp9.execute-api.us-east-1.amazonaws.com
# Local (alternativa):  http://localhost:4000   (cd server && node local.mjs)
