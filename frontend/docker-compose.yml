# Opcional: roda o backend fornecido localmente (store em memória, porta 4000).
# Alternativa sem Docker:  cd server && node local.mjs
services:
  api:
    image: node:22-alpine
    working_dir: /app
    command: node local.mjs
    ports:
      - "4000:4000"
    volumes:
      - ./server:/app
